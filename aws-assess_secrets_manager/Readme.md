# Description
	Reads any secrets residing in secrets manager that are encrypted with default KMS keys

# Requirements
	1. Role or user executing this script, must have the default FullReadOnly permission
	2. requirements.txt dependencies

# Execution
	1. python3 script.py -h